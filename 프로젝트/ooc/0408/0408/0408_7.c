//n을 입력받고, n개의 숫자를 입력받으시오.
// n개의 숫자들 중 가장 큰 값을 출력하는 프로그램을 만드시오.
// 이때 n개의 숫자들은 모두 자연수이다. 0<=n

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int main() {
	int n;
	scanf("%d", &n);
	int t = 1;
	int max = 0;     //정답을 저장할 공간


	if (n >= 0) {
		while(t<=n){
			int num;
			scanf("%d", &num);
		
			t++;
		}
		printf("%d", max == );
	}


}